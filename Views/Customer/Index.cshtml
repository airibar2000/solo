
@using solo.ViewModels
@model solo.ViewModels.ListViewModel
<h1>Customer View</h1>

@{

    foreach (var element in Model.Listofcustomers)
        {

        <li>
            @*@Url.Action(@element.Name, "Detail", new { id = element.Id, name = element.Name });*@
            @Html.ActionLink(@element.Name, "Detail/" + @element.Id);


        </li>


            }


        }

