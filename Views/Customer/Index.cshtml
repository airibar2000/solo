
@model IEnumerable<solo.Models.Customer>
<h1>Customer View</h1>

@{

    foreach (var element in Model)
        {

    <li>
        @*@Url.Action(@element.Name, "Detail", new { id = element.Id, name = element.Name });*@
        @*@Html.ActionLink(@element.Name, "Detail/" + @element.Id,new { name = element.Name });*@
        @Html.ActionLink(@element.Name, "Detail",new { id = element.Id },null);


    </li>


            }


        }

